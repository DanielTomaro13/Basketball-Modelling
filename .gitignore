# PRIVATE — never commit. The BasketballModel C# project is personal/private code.
BasketballModel/
*.cs
*.csproj
*.sln
bin/
obj/

# Data caches & large artifacts (regenerated by the pipeline)
data/raw/
# ...except the incremental game-log caches (box scores): committing them
# is what keeps CI runs incremental instead of re-fetching every game
!data/raw/gamelogs-*.json
data/processed/
*.parquet
*.feather

# Python
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Local tooling
.claude/

# Secrets & logs
.env
scripts/*.log
